// Prisma v7 configuration file
// Move connection URL here so Prisma Migrate can read it (avoid `url` in schema.prisma)
// TypeScript config left for editors only; Prisma CLI will use prisma.config.mjs
const config = {}
export default config
